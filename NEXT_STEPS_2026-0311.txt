* When you start the next session, say:

Let's now set up the n8n MCP connection. I need to generate an API key 
from osugenesis.app.n8n.cloud — walk me through it.
 
And in this way, we'll resume our Setup Checklist from MCP_ARCHITECTURE.md:
                                                                  
  1. Generate n8n API key — I'll tell you exactly where to click in the n8n UI
  2. Create .mcp.json in this repo with your API key and URL
  3. Restart Claude Code so it picks up the new MCP server
  4. Verify connectivity — I'll list your existing workflows to prove the link works
  5. Build a no-op webhook workflow on n8n cloud (receives form data, logs it)
  6. Set NEXT_PUBLIC_N8N_WEBHOOK_URL in Vercel
  7. Test end-to-end — submit the form, check n8n execution logs.

#eof#
